global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: novigo-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8080"]
        labels: { service: novigo-api, env: stack }
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
